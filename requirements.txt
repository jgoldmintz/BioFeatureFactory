# BioFeatureFactory Core Dependencies
biopython>=1.79
pandas>=1.3.0
numpy>=1.20.0,<2
requests>=2.25.0
pysam>=0.19.0

# Pipeline-specific dependencies (install separately):
# - ViennaRNA: conda install -c bioconda viennarna (for RNAfold pipeline)
# - nsp3: pip install nsp3 (for NetSurfP3 pipeline)
# - signalp6: Download from DTU, pip install /path/to/signalp-6-package (for NetNGlyc)
