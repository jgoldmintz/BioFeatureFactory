# BioFeatureFactory Core Dependencies
biopython>=1.79
pandas>=1.3.0
numpy>=1.20.0,<2
requests>=2.25.0
pysam>=0.19.0
scipy>=1.7.0

# Pipeline-specific (install as needed)
# RNAfold pipeline — also installable via: conda install -c bioconda viennarna
ViennaRNA
# EVmutation pipeline
numba>=0.53
# Rare codon pipeline
networkx>=2.6
prody>=2.0

# Miranda — conda only, not pip-installable:
#   conda install -c bioconda miranda

# NetSurfP3 — install from cloned repo:
#   pip install -r NetSurfP3/nsp3/requirements.txt

# SignalP 6.0 — install from DTU package:
#   pip install /path/to/signalp-6-package
